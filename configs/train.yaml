train:
  tth:
    v_max: 20 # максимальная скорость
    max_angle_speed: 5 # максимальная угловая скорость
  private:
    place: [5, 15] # точка посадки для локатора и лазера - я вынес ее за пределы паровоза

